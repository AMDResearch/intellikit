{
  "mcpServers": {
    "uprof-profiler": {
      "command": "uv",
      "args": [
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "run",
        "uprof-profiler-mcp"
      ]
    }
  }
}